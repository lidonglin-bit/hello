public class baby {
    public static void main(String[] args) {
        String title = "hello.txt";
        String substring = title.substring(0, title.lastIndexOf("."));
        System.out.println(substring);   //hello
    }
}
